{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
    <h2>Statistiques des régions</h2>
	<p>Nombre de visiteurs et de délégués par région distincte</p>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Nombre de visiteurs</th>
                <th>Nombre de délégués</th>
                <th>Afficher la liste des visiteurs de cette région</th>
            </tr>
        </thead>
        <tbody>
            {% set inc = 0 %}
            {% for uneRegion in lesRegions %}
                <tr>
                    <td> {{ uneRegion.NomReg }} </td>
                    <td> {{ lesNombresDeVisiteurs[inc] }} </td>
                    <td> {{ lesNombresDeDelegues[inc] }} </td>
                    <td>
                        <a href="{{ path('affichageVisiteursRegion', { 'idReg': uneRegion.Id }) }}" class="btn btn-primary btn-md"> Détail <i class="fa fa-sign-in"></i></a>
                    </td>
                </tr>
                {% set inc = inc + 1 %}
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
